# quantumultx-scripts

Quantumult X related scripts, configs, rule lists, and receiver helpers.

## Current Active Entrypoints

- `91live_normal_ntfy.conf` / `91live_normal_ntfy.js`
- `duocai_rtmp_ntfy.conf` / `duocai_rtmp_ntfy.js`
- `byhz-rewrite.conf` / `byhz-hls.js`
- `fanqie_mobile_ntfy_private.conf` / `fanqie_mobile_ntfy_private.js`

## Repository Layout

- `docs/`: usage notes and handoff docs
- `snippets/`: reusable Quantumult X snippet templates
- root: active raw-link entry files kept flat for compatibility

## Notes

- Files that are still used by raw GitHub links stay in the repository root on purpose.
- Local export files and backup files are ignored by `.gitignore`.
